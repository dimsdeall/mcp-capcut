---
"capcut-mcp": minor
---

Six new tools: `add_subtitles` (SRT import), `open_draft` (edit saved drafts), `edit_segment`, `remove_segment`, `audio_fade`, and `set_background`. The server version reported over MCP now follows package.json.
